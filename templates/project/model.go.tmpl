// Package model holds the domain types shared by all layers.
package model

// User is the sample domain type that ships with the scaffold.
// Replace it with the types of your own domain.
type User struct {
	ID   string
	Name string
}
